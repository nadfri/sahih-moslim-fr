# 🧪 Configuration Environment - Tests Vitest

# =============================================================================
# AUTH SECRET - Même que development ou générer un nouveau
# =============================================================================
AUTH_SECRET=your-test-auth-secret-here

# =============================================================================
# GITHUB OAUTH (Peut être identique au development)
# =============================================================================
AUTH_GITHUB_ID=your-github-client-id
AUTH_GITHUB_SECRET=your-github-client-secret

# =============================================================================
# AUTH CONFIGURATION
# =============================================================================
AUTH_TRUST_HOST=true

# =============================================================================
# SUPABASE TEST DATABASE CONFIGURATION
# Pour les tests, vous pouvez :
# 1. Utiliser la même instance Supabase (recommandé pour simplicité)
# 2. Créer une instance Supabase dédiée aux tests
# 3. Utiliser un schéma séparé dans la même base
# =============================================================================

# URL de connexion avec pooling (pour tests)
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"

# URL directe pour les migrations de test
DIRECT_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"

# =============================================================================
# SUPABASE PUBLIC CONFIGURATION (Tests)
# =============================================================================
NEXT_PUBLIC_SUPABASE_URL=https://[PROJECT-REF].supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# =============================================================================
# SUPABASE CREDENTIALS
# =============================================================================
SUPABASE_PSW=your-database-password

# =============================================================================
# INSTRUCTIONS D'UTILISATION
# =============================================================================
# 1. Copiez ce fichier vers .env.test : cp .env.example.test .env.test
# 2. Remplacez toutes les valeurs par vos vraies credentials
# 3. Pour les tests, vous pouvez utiliser la même instance Supabase
#    que le développement (les tests ne modifient pas les données permanentes)
# 4. Assurez-vous que .env.test est dans .gitignore (déjà fait ✅)
