# 🕋 Configuration Environment - Sahih Moslim FR

# =============================================================================
# AUTH SECRET - Générer avec: openssl rand -base64 32
# =============================================================================
AUTH_SECRET=your-super-secret-auth-key-here

# =============================================================================
# GITHUB OAUTH (Optionnel pour l'authentification)
# =============================================================================
AUTH_GITHUB_ID=your-github-client-id
AUTH_GITHUB_SECRET=your-github-client-secret

# =============================================================================
# AUTH CONFIGURATION
# =============================================================================
AUTH_TRUST_HOST=true

# =============================================================================
# SUPABASE DATABASE CONFIGURATION
# Remplacer [PROJECT-REF], [PASSWORD], et [REGION] par vos vraies valeurs
# =============================================================================

# URL de connexion avec pooling (recommandé pour production)
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"

# URL de migration (shadow database pour les migrations Prisma)
SHADOW_DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"

# =============================================================================
# SUPABASE PUBLIC CONFIGURATION
# =============================================================================
NEXT_PUBLIC_SUPABASE_URL=https://[PROJECT-REF].supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# =============================================================================
# SUPABASE CREDENTIALS (Service Role Key - for server-side operations)
# =============================================================================
SUPABASE_PSW=your-database-password
SUPABASE_URL=https://[PROJECT-REF].supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
PROJECT_ID=[PROJECT-REF]

# =============================================================================
# INSTRUCTIONS D'UTILISATION
# =============================================================================
# 1. Copiez ce fichier vers .env : cp .env.example .env
# 2. Remplacez toutes les valeurs par vos vraies credentials Supabase
# 3. Pour obtenir vos credentials Supabase :
#    - Allez sur https://supabase.com/dashboard
#    - Sélectionnez votre projet
#    - Settings > Database > Connection string (DATABASE_URL et SHADOW_DATABASE_URL)
#    - Settings > API > Project URL et anon public key
#    - Settings > API > Service Role Key (SUPABASE_SERVICE_ROLE_KEY)
# 4. Générez AUTH_SECRET avec : openssl rand -base64 32
# 5. Pour GitHub OAuth (optionnel) :
#    - https://github.com/settings/applications/new
#    - Authorization callback URL: http://localhost:3000/api/auth/callback/github